providers = ["node"]

[phases.install]
cmds = ["npm ci --omit=dev --no-audit --no-fund"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[phases.setup]
cmds = []

[start]
cmd = "bash start.sh"
